#!/bin/bash
# Neovimペインの起動スクリプト (サーバーモード)

cd "${1:-$PWD}"

# ソケットパス
SOCKET_PATH="/tmp/nvim-ghostty-${PPID}.sock"
echo "$SOCKET_PATH" > /tmp/nvim-ghostty-socket

# Neovim起動
exec nvim --listen "$SOCKET_PATH" "${2:-.}"
